{
  "name": "@sentinella/api-client",
  "version": "0.0.1",
  "private": true,
  "description": "HTTP client para o frontend Sentinella — substitui supabase.from() e supabase.auth",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@sentinella/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
