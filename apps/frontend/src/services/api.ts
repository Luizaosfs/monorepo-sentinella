/**
 * api.ts — Ponto de entrada público da camada de serviços.
 *
 * A lógica foi decomposta em módulos por domínio em:
 *   src/services/api/domains/
 *
 * Helpers compartilhados em:
 *   src/services/api/shared/
 *
 * Este arquivo mantém a interface pública original:
 *   import { api } from '@/services/api'
 */
export { api } from './api/index';
