model audit_log {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  cliente_id  String?  @db.Uuid
  usuario_id  String?  @db.Uuid
  auth_uid    String?  @db.Uuid
  acao        String
  tabela      String?
  registro_id String?  @db.Uuid
  ip_hash     String?
  descricao   String?
  payload     Json?
  dados_antes  Json?   // PENDENTE: coluna existe no Prisma mas não no banco — avaliar se é adição futura ou drift a reverter
  dados_depois Json?   // PENDENTE: coluna existe no Prisma mas não no banco — avaliar se é adição futura ou drift a reverter
  operacao    String?  // PENDENTE: coluna existe no Prisma mas não no banco — avaliar se é adição futura ou drift a reverter
  created_at  DateTime @default(now()) @db.Timestamptz

  @@index([cliente_id])
  @@index([cliente_id, created_at])
  @@index([auth_uid])
  @@index([tabela, registro_id, created_at])
  @@map("audit_log")
  @@schema("public")
}
