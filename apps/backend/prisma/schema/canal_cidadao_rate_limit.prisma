model canal_cidadao_rate_limit {
  ip_hash     String
  cliente_id  String   @db.Uuid
  janela_hora DateTime @default(now()) @db.Timestamptz
  contagem    Int      @default(1)

  @@id([ip_hash, cliente_id, janela_hora])
  @@map("canal_cidadao_rate_limit")
}
